# sources.candidates.txt — a wider pool to (re)validate with:
#   python -m src.ingestion.validate sources.candidates.txt --out sources.txt
#
# The lines below COULD NOT be confirmed live on 2026-06-20 — the API returned
# an empty/zero-posting response, which can mean a different token, a board with
# no current public postings, or a company that has moved off this ATS. Uncomment
# and validate before trusting any of them.

# greenhouse coinbase
# greenhouse doordash
# greenhouse hashicorp
# greenhouse confluent
# lever netflix      # returned [] (token resolves, no public postings)
# lever plaid        # returned []
# lever attentive    # returned []
# lever ramp
# lever kayak

# Tip: paste a careers URL directly and the parser detects the ATS + token, e.g.
# https://job-boards.greenhouse.io/yourcompany
# https://jobs.lever.co/yourcompany
