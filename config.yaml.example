# Copy this file to config.yaml and fill in your own values.
# config.yaml is gitignored — it holds your personal search criteria and
# resume summary, not something you commit.

# ── What you're looking for ─────────────────────────────────────────────────
# Shared across every source. For SEEK, titles x locations become searches.
# For ATS boards (Greenhouse/Lever/Ashby), they filter the fetched postings.
# Empty lists mean "no filter". A posting in a "remote" location always passes
# the location filter.
filters:
  titles:    ["data analyst", "data scientist"]
  locations: ["Adelaide", "Sydney"]

# ── Where to look ───────────────────────────────────────────────────────────
# Sources are just "where". ATS boards (greenhouse/lever/ashby) are global —
# they return a company's whole list and the location filter above keeps the
# ones you want, so non-AU roles (e.g. India: "Bangalore", "Mumbai") come
# through here. SEEK, by contrast, is an Australia/NZ board, so scope its
# *search* locations with `locations:` (otherwise it uses filters.locations and
# wastes searches on non-AU cities that return nothing).
sources:
  - type: seek
    locations: ["Adelaide", "Sydney"]   # AU/NZ only; omit to use filters.locations
  - type: greenhouse
    board: "stripe"                  # token in boards.greenhouse.io/stripe
  - type: lever
    company: "figma"                 # token in jobs.lever.co/figma
  - type: ashby
    org: "ashby"                     # token in jobs.ashbyhq.com/ashby
  - type: adzuna
    country: "au"                    # one per country; needs the adzuna: creds below

# Optionally keep a long list of boards in a separate file (see
# sources.txt.example). Its sources are appended to the ones above.
# sources_file: "sources.txt"

# Legacy: a single SEEK search. Still works — it's treated as one seek source
# plus a one-title/one-location filters block. Prefer filters + sources above.
# search:
#   title: "data analyst"
#   location: "Adelaide"

llm:
  backend: ollama               # only "ollama" is implemented today
  model: llama3                 # must be pulled already: `ollama pull llama3`
  host: http://localhost:11434

# ── Adzuna search API (optional) ────────────────────────────────────────────
# Sanctioned job-search API with a free tier; per-country (au, in, gb, ...), so
# it reaches non-AU cities SEEK can't. Register free at developer.adzuna.com,
# then add `adzuna au` / `adzuna in` sources. Omit this block to skip Adzuna.
# adzuna:
#   app_id: "YOUR_ADZUNA_APP_ID"
#   app_key: "YOUR_ADZUNA_APP_KEY"

# ── Where to store it ───────────────────────────────────────────────────────
# Optional. Defaults to sqlite:///data/jobs.db. SQLite is the product; a
# Postgres URL is an open-but-unverified escape hatch (see docs/configuration.md).
# Can also be set via the JOBHUNTER_DATA